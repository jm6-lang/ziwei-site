name = "ziwei-site"
compatibility_date = "2024-01-01"
pages_build_output_dir = "./dist"

# 自动部署配置
# 当推送到 main/master 分支时，Cloudflare Pages 会自动构建和部署
# 无需手动运行命令

# 环境变量（在 Cloudflare Dashboard 中配置更安全）
# [vars]
# ENVIRONMENT = "production"

# 如果需要自定义域名，在 Cloudflare Pages 项目中配置
# domains = ["ziwei.example.com"]
